FROM debian:stretch

RUN apt-get -yy update && apt-get -yy install git curl bash build-essential

RUN curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y

ENV PATH="/root/.cargo/bin:${PATH}"
